export class Media {
    constructor(mediaName, artist, source, mediaId, requestedBy) {
        this.mediaName = mediaName;
        this.artist = artist;
        this.source = source;
        this.mediaId = mediaId;
        this.requestedBy = requestedBy;
    }
}